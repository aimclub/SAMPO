#include <set>
#include <string>

#include "native/schemas/sorted_list.h"

using namespace std;

template <typename T>
size_t EventSortedList<T>::bisect_right(const ScheduleEvent<T>& value) {

}

template <typename T>
size_t EventSortedList<T>::bisect_left(const ScheduleEvent<T>& value) {

}

template <typename T>
ScheduleEvent<T>& EventSortedList<T>::operator[](size_t i) {

}
