file(
    GET_RUNTIME_DEPENDENCIES
    LIBRARIES @PACKAGE_TARGETS@
    RESOLVED_DEPENDENCIES_VAR RESOLVED_DEPS
    UNRESOLVED_DEPENDENCIES_VAR UNRESOLVED_DEPS
    PRE_INCLUDE_REGEXES @RUNTIME_DEPS_INCLUDE@
    PRE_EXCLUDE_REGEXES @RUNTIME_DEPS_EXCLUDE@
    POST_INCLUDE_REGEXES @RUNTIME_DEPS_INCLUDE@
    POST_EXCLUDE_REGEXES @RUNTIME_DEPS_EXCLUDE@
    DIRECTORIES @RUNTIME_DEPS_PATHS@
)

foreach(_file ${RESOLVED_DEPS})
    file(INSTALL DESTINATION @BINARY_DIR@ TYPE SHARED_LIBRARY FOLLOW_SYMLINK_CHAIN FILES "${_file}")
endforeach()